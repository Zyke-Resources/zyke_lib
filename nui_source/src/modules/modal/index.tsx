import "./modal.css";

/**
 * Modal Module
 *
 * Provides the reusable Modal component and ModalContext.
 * Individual features (input dialog, confirm dialog, etc.) will be built on top of this.
 *
 * The ModalProvider is placed in App.tsx so all modules can access it.
 * This module is a placeholder entry for any future global modal listeners.
 */
const ModalModule = () => {
	return null;
};

export default ModalModule;
